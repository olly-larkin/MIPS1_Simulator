#!/bin/bash

FILES="tbench_src/text_files/*"
mkdir -p test/temp

FAILED=()

for F in $FILES
do
    parser/bin/parser $F "test/temp/#"
    if [ $? -eq 5 ] 
    then
        FAILED+=($F)
    fi
done

echo -e "Finished.\n"

for i in ${FAILED[*]}; do
    echo "Failed creation: " $i
done

echo